Compact uppercase status badge in mono type — for states, counts, and labels.

```jsx
<Badge variant="success" dot>Live</Badge>
<Badge variant="accent">Beta</Badge>
<Badge variant="warning">Degraded</Badge>
```

Variants: `neutral`, `accent`, `success`, `warning`, `danger`, `solid`. Set `dot` for a leading status dot.
